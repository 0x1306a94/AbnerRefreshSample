
export class CommonConstant {
  /**
   * Full the width.
   */
  static readonly FULL_WIDTH: string = '100%';
  /**
   * Full the height.
   */
  static readonly FULL_HEIGHT: string = '100%';
  /**
   * 相对容器默认名称
   */
  static readonly RELATIVE_CONTAINER: string = '__container__';
}